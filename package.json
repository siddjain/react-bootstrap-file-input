{
  "name": "@siddjain/react-bootstrap-file-input",
  "version": "0.1.0",
  "description": "react file picker that allows user to select multiple files and order or delete them from the selection. uses bootstrap for styling.",
  "main": "dist/FileInput.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "rm -rf ./dist && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/siddjain/react-bootstrap-file-input.git"
  },
  "bugs": {
    "url": "https://github.com/siddjain/react-bootstrap-file-input/issues"
  },
  "homepage": "https://github.com/siddjain/react-bootstrap-file-input#readme",
  "keywords": [
    "file",
    "picker"
  ],
  "author": "Siddharth Jain",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.15",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  },
  "peerDependencies": {
    "bootstrap": ">=5.0.0",
    "react": ">=17.0.0",
    "react-bootstrap": ">=2.0.0",
    "react-dom": ">=17.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "src/**/*.tsx",
    "dist/**/*.d.ts*",
    "dist/**/*.js",
    "dist/**/*.map"
  ]
}
